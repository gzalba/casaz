import React from "react";
import ReactDOM from "react-dom/client";
import PortalCasa from "./PortalCasa.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PortalCasa />
  </React.StrictMode>
);
